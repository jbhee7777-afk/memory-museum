# 검증 결과

검증일: 2026-09-18

## 2026-09-19 삭제·바로 이동·노트북 카메라 수정

Microsoft Edge 가상 카메라와 독립된 테스트 저장소로 다음 항목을 확인했습니다.

- 확대 작품 창에서 ‘이 작품 삭제’ → 삭제 확인 → 실제 벽·저장소에서 삭제: 통과. 삭제 확인창을 연 뒤 기다려도 취소 처리되지 않음을 확인했습니다.
- ‘작품 삭제’ 사진 목록에서 선택 → 취소 시 보존 / 확인 시 삭제: 통과.
- 새로고침 후 삭제 상태 유지: 통과.
- 40칸 전시관의 ‘맨 뒤 액자’ → 40번 액자, 번호 선택 → 20번 액자 이동: 통과.
- 노트북·전면 카메라 선택 시 `facingMode.ideal=user` 전달, 가상 카메라로 수동 촬영 → 벽 등록: 통과.
- 모바일 390×844 화면에서 맨 뒤 액자 이동, 사진 목록 삭제, 가로 넘침 없음: 통과.
- 처리되지 않은 JavaScript 오류: 없음.

실제 노트북 웹캠/휴대폰 카메라 하드웨어는 연결해 검증하지 않았습니다. 브라우저 권한과 운영체제 카메라 설정은 기기에서 확인해야 합니다.

## 이미지 전시관 업데이트 검증

새 전시관 UI를 별도 브라우저 컨텍스트에서 점검했습니다.

- 전시관 사진 배경, 사진으로 된 벽·액자·바닥 로드 및 시각 확인: 통과.
- 업로드 직후 큰 이미지가 4초 시점에도 표시되는지 확인: 통과.
- 이미지 로드 완료부터 확대 감상 5초 + 벽으로 이동 약 1.25초: 실제 전체 약 6.3초 확인.
- 올바른 번호의 실제 벽 이미지에 작품이 들어가는지 확인: 통과.
- 다음 작품 버튼으로 반대쪽 벽에 이동: 통과.
- 방향키를 눌러 카메라 위치가 복도 안쪽으로 이동: 통과.
- 전체 모아 보기와 걷기 전환 후 작품 유지: 통과.
- 390×844 모바일의 조작 버튼과 가로 넘침 없음: 통과.
- 신규 화면의 처리되지 않은 JavaScript 오류: 없음.
- 가상 카메라의 수동 촬영 → 5초 감상 → 벽에 걸기, 사진 추가, 자동 산책, 테마 변경 후 작품 무필터, 새로고침 복원, 벽의 작품 클릭 확대: 통과.

아래는 앞선 버전의 촬영·저장·보조 기능 검증 기록입니다. 새 버전에서도 동일한 저장소와 촬영 모듈을 사용합니다.

환경: Windows, 로컬 HTTP 정적 서버, Microsoft Edge 기반 Playwright 자동화. 카메라 테스트에는 브라우저의 가상 카메라 및 Canvas MediaStream을 사용했습니다. 최종 런타임에서 QR 디코더를 대체하지 않고 실제 jsQR로 생성된 QR 영상을 인식시켰습니다.

## STEP 1 — 핵심 기능 구현

시작/이어하기, IndexedDB, 카메라 선택, QR 인식, 카운트다운, 중앙 가이드 잘라내기, 수동 촬영, 사진 추가, 전시, 작품 삭제를 먼저 구성했습니다.

## STEP 2 — MVP 검증

| 점검 | 결과 |
|---|---|
| 작품 수 3개 설정 → 정확히 3칸 생성 | 통과 |
| 가상 카메라 시작과 영상 크기 확보 | 통과 |
| 수동 촬영 3초 카운트다운 → 이미지 등록 | 통과 |
| 새로고침 → 작품과 설정 복원 | 통과 |
| 테마 변경 → 기존 작품 유지 | 통과 |
| 마지막 작품 삭제 → 빈 칸 유지 | 통과 |
| 모바일 가로 넘침 확인 | 통과 |
| 공통 QR 생성 → 실제 jsQR 디코딩 | 통과 |

## STEP 3 — 테마와 추가 기능 검증

| 점검 | 결과 |
|---|---|
| Canvas 영상의 공통 QR → 3초 자동 촬영 | 통과 |
| 동일 QR을 계속 유지 → 추가 촬영 없음 | 통과 |
| QR을 치웠다가 재등장 → 다음 빈 칸 등록 | 통과 |
| 카운트다운 중 QR 제거 → 촬영 취소 | 통과 |
| EXHIBIT-04 → 4번 칸 배치, 3번 빈 칸 보존 | 통과 |
| 뒤쪽에 작품이 있을 때 작품 수 축소 차단 | 통과 |
| 잘못된 PNG 내용 → 오류 안내, 작품 수 유지 | 통과 |
| 저장 공간 부족 예외 주입 → 기존 상태 유지 | 통과 |
| 파일 추가 → 선택 이름 저장 → 완료 화면 | 통과 |
| 번호 QR 4개 생성 및 각각 디코딩 | 통과 |
| QR PNG 다운로드 및 인쇄용 이미지 준비 | 통과 |
| 전시관 PNG 실제 다운로드 | 통과 |
| 3초 감상 전환, 일시정지, 다음, 종료 | 통과 |
| 네 테마 전환과 작품 보존 | 통과 |
| 새로고침 후 이름·이미지·테마 복원 | 통과 |
| 동일 브라우저의 두 번째 탭 편집 차단 | 통과 |
| 새 전시관 취소 → 기존 작품 유지 | 통과 |
| 카메라 권한 거부 오류 → 한글 안내 | 통과 |
| 전체가 흰 배경 → 문서 자동 보정 거절, 가이드로 처리 | 통과 |
| 처리되지 않은 브라우저 JavaScript 오류 | 없음 |

## STEP 4 — 수업 UX 및 최종 수정 검증

- 1600×900 화면의 40칸 모아 보기, 카메라 패널 숨김을 확인했습니다.
- 전체화면 진입과 종료를 확인했습니다.
- 390×844 모바일에서 한 화면 모드 대신 읽기 쉬운 2열 스크롤 배치를 확인했습니다.
- 모바일에서 글자 크기 200%로 확대했을 때 가로 넘침이 없는지 확인했습니다.
- 촬영 도중 자동 촬영 OFF를 눌러 카운트다운이 취소되고 OFF 설정이 저장되는지 확인했습니다.
- 새 전시관 확인 → 새 제목과 1칸 설정 → 실제 교체를 확인했습니다.
- 새 전시관 설정 도중 기존 전시관 이어하기가 가능한지 확인했습니다.
- 합성 사다리꼴 흰 종이의 네 꼭짓점 추정과 원근 보정 결과를 확인했습니다.
- 가장자리의 작은 QR을 가린 뒤 실제 디코더로 QR이 더 이상 인식되지 않음을 확인했습니다.
- 카메라 없음 오류에 대해 한글 안내가 나오는지 확인했습니다.
- 데스크톱·모바일·테마별 스크린샷을 확인하여 작은 화면의 글자 겹침과 버튼 줄바꿈을 수정했습니다.

## 실제 장비로 확인해야 하는 범위

**실제 USB 실물화상기·전자칠판과 교실 조명에서는 아직 테스트하지 않았습니다.** 따라서 해당 장비의 영상 품질이나 학교 권한 정책까지 확인된 것으로 볼 수 없습니다.

실물화상기의 드라이버 이름, 자동초점, 종이 반사, 어두운/색종이, 배경색, QR 인쇄 품질, 극단적인 원근 왜곡은 현장에서 확인해야 합니다. 자동 문서 보정은 밝은 종이를 위한 보조 기능이며 모든 문서에서 성공을 보장하지 않습니다. 실패하면 중앙 가이드로 처리합니다.

Safari·Firefox·iOS·Android 실기기와 장시간 연속 수업은 별도로 검증하지 않았습니다. 인쇄는 인쇄용 이미지 준비까지 확인했으며 실제 프린터 출력은 하지 않았습니다. GitHub 계정에 실제 배포하지는 않았습니다. 포함된 안내서의 배포 후 테스트와 실물화상기 현장 테스트를 진행하세요.

자동 검증은 앱 데이터가 없는 별도 브라우저 컨텍스트에서 수행했습니다. 검증용 합성 이미지나 테스트 작품은 배포 파일 및 사용자의 브라우저 저장소에 포함하지 않았습니다.
